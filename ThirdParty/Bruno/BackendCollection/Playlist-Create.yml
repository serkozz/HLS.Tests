info:
  name: Playlist/Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/playlist/create"
  body:
    type: json
    data: '"Yooh_-_MariannE.mp3"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
